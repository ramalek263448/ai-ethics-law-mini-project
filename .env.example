# Wzorzec konfiguracji – skopiuj do `.env` i uzupełnij własnymi wartościami.
# Plik `.env` NIE powinien trafić do repozytorium (dodaj do .gitignore).

OLLAMA_HOST=http://localhost:11434
OLLAMA_TIMEOUT=120
LLM_GUARD_THRESHOLD=0.5
